import { SafeTranslatePipe } from './safe-translate.pipe';

describe('SafeTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new SafeTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
